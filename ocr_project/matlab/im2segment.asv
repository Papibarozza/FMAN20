function [S] = im2segment(im)
% [S] = im2segment(im)

nrofsegments = 5;
m = size(im,1);
n = size(im,2);
im_segment = imcomplement(im(:,0:50));

S{1} = [imcomplement(im(:,0:50)) zeros(m,n-50)]
for i = 2:(nrofsegments)
    im_segmen
    S{i}= [zeros(m,30*(k-1)) imcomplement(im(:,0:30)) zeros(m,n-50-30*(k-1))]./255;
end
